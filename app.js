// Chapter 1 (Alerts)

// Q1. Alert these following (individually):

// alert("First Name");
// alert("Last Name");
// alert("Email");
// alert("Phone Number");
// alert("Password");

// Q2. Correct this statement: alert"You're learning JavaScript!";

// alert("You're learning JavaScript!");

// Q3. Code an alert statement displaying any message you like.

// alert("Hello World!");

// Chapter 2 (Variables for string)

// Q1. Declare any variable in the camelCase format.

// var firstName;

// Q2. Declare a variable of your choice without defining it. Then, in a 
// second statement, assign it a string of your choice.

// var a;
// a = "Hello World";

// 3. Declare the variable teamName and Alert your Team name.

// var teamName = "XYZ";
// alert(teamName);

// 4. This statement has already been coded. var bestMan = "Charlie";
// Assign the variable a new string.

// var bestMan = "Charlie";
// bestMan = "Ahmed";

// Chapter 3 (Variables for numbers)

// Q1. Declare a variable “caseQty”

// var caseQty;

// Q2. Assign to the variable caseQty, which has already been declared,
// the value 144.

// caseQty = 144;

// Q3. Rewrite this statement so the variable can be used in a math
// operation. var num = "9";

// var num = 9;

// Q4. In one statement declare a variable. In a second statement assign
// it the sum of 2 numbers.

// var a;
// a = 2 + 2;

// Q5. What is the value of orderTotal?
// var merchTotal = 100;
// var shippingCharge = 10;
// var orderTotal = merchTotal + shippingCharge;
// Try it yourself.

// var merchTotal = 100;
// var shippingCharge = 10;
// var orderTotal = merchTotal + shippingCharge;
// alert(orderTotal);

// Q6. In the first statement declare a variable and assign it a number. In
// the second statement, change the value of the variable by adding
// it together with a number.

// var c = 10;
// c = c + 2;

// Chapter 4 (Variable names Legal and Illegal)

// Q1. Correct this statement.
// var product cost = 3.45;

// var productCost = 3.45;

// Q2. Rewrite this using camelCase.
// var Nameofband;

// var nameofBand;

// Q3. In a single statement declare a legally-named variable and assign a
// number to it.

// var myNum = 10;

// Q4. Declare a variable that is a combination of your first and last
// names. Use camelCase.

// var muhammadShariq;

// Q5. List the legal and Illegal Variables.

// Legal Variables
// var firstName;
// var _helloVar;
// var $name;
// var myName123;
// var user_name;

// Illegal Variables
// var 123num;
// var my-city;
// var class;
// var a&b;
// var function;

// Chapter 5 (Math Expression I)

// Q1. What is the name and symbol of the arithmetic operator that
// gives you the remainder when one number is divided by another?

// modulo operator.

// Q2. What is the value of num?
// var num = 20 % 6;

// The value of num is "2".

// Q3. In a single statement, declare the variable largeNum and assign it
// the result of 1,000 multiplied by 2,000.

// var largeNum = 1000 * 2000;

// Q4. Assign to a variable the value represented by one variable
// subtracted from the value represented by another variable.

// var num1 = 40;
// var num2 = 20;
// var result = num1 - num2;

// Q5. Assign to a variable the remainder when one number is divided by
// another. The variable hasn't been declared beforehand. Make up
// the variable name.

// var remainder = 10 % 3;

// Q6. Code an alert that displays the result of a multiplication on 2
// numbers.

// alert(2*4);

// Chapter 6 (Math Expression II)

// Q1. Code a short form of x = x + 1; Use either of the two legal
// expressions.

// x++;

// Q2. If x has a value of 100, what is the fastest way to reduce it to 99
// with a math expression?

// x--;

// Q3. var x = 50;
// var y = x++;
// What is the value of y?

// The value of y is "50".

// Q4. var y = 50;
// var z = --y;
// What is the value of z?

// The value of z is "49".

// Q5. In a single statement, decrement num and assign its original value
// to newNum.

// var num = 11;
// var newNum = num--;

// Q6. In a single statement add 1 to a variable and assign its original
// value to another variable.

// var variable1 = 9;
// var variable2 = variable1++;

// Q7. Assign a number value to a variable.
// Increment the variable.
// Display the new value in an alert

// var a = 4;
// a++;
// alert(a);

// Chapter 7 (Math Expression III)

// Q1. var calculatedNum = 2 + (2 * 6);
// What is the value of calculatedNum?

// The value of calculatedNum is "14".

// Q2. var calculatedNum = (2 + 2) * 6;
// What is the value of calculatedNum?

// The value of calculatedNum is "24".

// Q3. var calculatedNum = (2 + 2) * (4 + 2);
// What is the value of calculatedNum?

// The value of calculatedNum is "24".

// Q4. var calculatedNum = ((2 + 2) * 4) + 2;
// What is the value of calculatedNum?

// The value of calculatedNum is "18".

// Q5. Write a statement that assigns to cost the result of 2 + 2 * 4 + 10,
// clarified with parentheses, producing 56.

// var cost = (2 + 2) * (4 + 10);

// Q6. Write a statement that assigns to units the result of 2 + 2 * 4 + 10,
// clarified with parentheses, producing 20.

// var units = 2 + (2 * 4) + 10;

// Q7. Write a statement that assigns to pressure the result of 4 / 2 * 4,
// clarified with parentheses, producing 5.

// var pressure = 4 / (2 * 4);

// Chapter 8 (Concatenating Text Strings)

// Q1. var num = "2" + "2";
// What is the value of num? Include quotation marks.

// The value of num is "22"

// Q2. message = ("Hello," + "Dolly");
// What is the value of message? (Include the quotation marks.)
// Alert the statement

// message = ("Hello," + "Dolly");
// alert(message);

// Q3. alert("33" + 3);
// What message displays in the alert box?

// Message displays in the alert box is "333".

// Q4. Write an alert that displays the concatenation of the two parts of
// "Pakistan Zindabad".

// alert("Pakistan" + "Zindabad");

// Q5. Write a statement that assigns to a variable the concatenation of
// a string with a number

// var concatenate = "This is a number:" + 10;

// Q6. Assign strings to two variables. Then concatenate them and assign
// the result to a third variable.

// var a = "Hello";
// var b = "World";
// var result = a + b;

// Chapter 9 (Prompts)

// Q1. Code a prompt with the message "Enter first name". The user's
// response is assigned to firstName.

// var firstName = prompt("Enter first name");

// Q2. Code a prompt with the message "Country?" and the default
// answer "China". The user's response is assigned to country.

// var country = prompt("Country?","China");

// Q3. Correct this statement
// var yourName = prompt(Enter Your Name");

// var yourName = prompt("Enter Your Name");

// Q4. Code a prompt that specifies a string as the message Include a
// default input.

// var city = prompt("Enter Your City","Karachi");

// Q5. Assign strings to two variables. Code a prompt specifying the first
// variable as the message and the second variable as the default
// response. Assign the user's response to a third variable.

// var a = "Enter Your Country Name";
// var b = "Pakistan";
// var country = prompt(a,b);

// Q6. Display a prompt, including both a message and a default
// response.
// Display the user's response in an alert.

// var userInp = prompt("Enter Your Email","example@gmail.com");
// alert(userInp);

// Chapter 10 (if statments)

// Q1. var city = "Karachi"
// if (city = "Karachi") {
// console.log("The City OF Lights")
// Correct the above statement:
// Also try this statement by yourself

// var city = "Karachi";
// if (city = "Karachi") {
//     console.log("The City Of Lights");
// }

// Q2. This is the first line of an if statement:
// if (x === y) {
// Complete the statement. If the condition is true, display a box
// that asks the user value of z? and assign it to another variable.

// var x = 10;
// var y = x;
// if (x === y) {
//     var z = prompt("Enter any Value");
// }

// Q3. Code an if statement that tests if ZipCode is "10010" so, Alert
// that "Karachi". if not then alert ("Please write correct city")

// var zipCode = prompt("Enter a ZipCode");
// if (zipCode === "10010") {
//    alert("Karachi"); 
// }
// else {
//     alert("Please write correct city");
// }

// Q4. Code an if statement. Test whether a variable has a particular
// numerical value. If so, assign a new value to that variable, as in
// x = 1;

// var x = 1;
// if (x === 1) {
//   x = 2;
// }
// console.log(x);

// Chapter 11 (Comparison Operators)

// Q1. Code the first line of an if statement that tests whether one
// variable is unequal to another. (Use !)

// var a = 1;
// var b = 10;
// if (a !== b) {

// }

// Q2. Code the first line of an if statement that tests whether the
// value represented by a variable is greater than or equal to the
// value represented by another variable.

// var a = 2;
// var b = 12;
// if (a <= b) {
//     alert("hello");
// }

// Q3. Code an if statement. Test whether a variable is unequal to a
// particular number. If so, assign a number to that variable.

// var i = 10;
// if (i !== 20) {
//     i = 20;
// }

// Q4. Code an if statement that tests whether a number is unequal to
// a different number. If the condition is true (it will be), display a
// congratulations alert.

// var h = 100;
// if (h !== 50) {
//     alert("congratulations");
// }

// Q5. Code a prompt asking for your first name.
// Code an if statement that tests whether the name you entered
// is unequal to another name.
// If the condition is true (it will be), display an alert that says "No
// match"

// var firstName = prompt("Enter your first name");
// var otherName = "ali";
// if (firstName !== otherName) {
//     alert("No match");
// }

// Chapter 12 (if…else and else if statements)

// Q1. Code an if statement that tests whether the value represented by
// a variable is greater than or equal to the value represented by
// another variable. If so, display an alert. If not, display a different
// alert.

// var a = prompt("Enter a number");
// var b = "60";
// if (a >= "60") {
//     alert("congratulation");
// }
// else {
//     alert("Fail");
// }

// Q2. Write a program using if else and else if statement which take
// marks from user. And the program will calculate your percentage
// and give you grade A/C to Your percentage. (MARKSHEET)

// var userMarks = prompt("Enter your marks:");
// var percentage = (userMarks / 100) * 100;
// var grade;
// if (percentage >= 90) {
//     grade = "A+";
//   } else if (percentage >= 80) {
//     grade = "A";
//   } else if (percentage >= 70) {
//     grade = "B";
//   } else if (percentage >= 60) {
//     grade = "C";
//   } else if (percentage >= 50) {
//     grade = "D";
//   } else {
//     grade = "F";
//   }
// alert("Your grade is: " + grade);

// Q3. This is the if statement that begins the code.
// if (a === 10) {
//  alert("a is 10");
// }
// If a isn't 10, display an alert that says The correct value of a is
// ___

// if (a === 10) {
//     alert("a is 10");
// } else {
//     alert("The correct value of a is " + a);
// }

// Q4. Prompt the user to enter a city.
// If the city is Karachi, display an alert acknowledging it is Karachi.
// If not, check to see if it's Lahore.
// If it is, display an alert acknowledging it's Lahore.
// Otherwise, display a different alert.

// var city = prompt("Enter your city name");
// if (city === "Karachi") {
//     alert("You entered Karachi");
// } else if (city === "Lahore") {
//     alert("You entered Lahore");
// } else {
//     alert("Beautiful City");
// }

// Chapter 13 (Testing sets of conditions)

// Q1. Code the first line of an if statement that tests whether both are
// true: a has the same value as b and c has the same value as d.


// if (a === b && c === d) {
//     alert("equal");
// }


// Q2. Code the first line of an if statement that tests whether either or
// both are true: a has the same value as b or c doesn't have the
// same value as d.


// if (a === b || c !== d) {
//     alert("true");
// }

// Q3. Code the first line of an if statement that tests whether
// I. name is either "Hamza" or "Arsalan".
// II. age is Less than 60.

// if ((Name === "Hamza" || Name === "Arsalan") && age < 60 ) {
//    alert("true");
// }

// Q4. Declare two variables and assign them number values.
// If the first variable is less than the second variable or greater than
// the second variable, display an alert.

// var a = 10;
// var b = 20;
// if (a < b || a > b) {
//     alert("true");
// }

// Q5. Declare 2 variables. Assign one of them your first name and the
// other one your last name.
// Code 2 prompts, asking for your first and your last name.
// If your answers match the two variables, display an alert.

// var firstName = "Muhammad";
// var lastName = "Shariq";
// var name1 = prompt("Enter your first name");
// var name2 = prompt("Enter your last name");
// if (name1 === firstName && name2 === lastName) {
//     alert("true");
// }

// Chapter 14 (If statements nested)

// Q1. Code an if statement enclosing a nested if. If password is not
// empty, then check if password is not greater than 5 , then display
// an alert that says "Password must be greater than 5" if greater
// than 5 then Alert "OK".

// var password = prompt("Enter a password");
// if (password !== "") {
//     if (password.length < 5) {
//         alert("Password must be greater than 5");
//     } else if (password.length > 5) {
//         alert("OK");
//     }
// }

// Q2. Try this statement by yourself
// if (a === 1) {
// if (c === "Max") {
//  alert("OK");
//  }
// }

// var a = 1;
// var c = "Max";
// if (a === 1) {
//   if (c === "Max") {
//    alert("OK");
//   }
// }

// Q3. Code the first line of an if statement that avoids the nesting above
// by testing for multiple conditions.
// if (a === 1) {
//  if (c === "Max") {
// alert("OK");
//  }
// }

// var a = 1;
// var c = "Max";
// if (a === 1 && c === "Max") {
//    alert("OK");
// }

// Q4. Declare two variables and assign them the same number value.
// Test two conditions, using nested if statements. Test whether the
// first variable equals the second variable and also whether it is less
// than or equal to the second variable. If the test passes—and it
// will—display an alert message.

// var a = 10;
// var b = 10;
// if (a === b) {
//     if (a <= b) {
//         alert("OK");
//     }
// }

// Chapter 15 (Array I)

// Q1. Declare an empty array.

// var a = [];

// Q2. Code an array with 1 string element

// var animals = ["cat"];

// Q3. var alphabet = ["h","i","j","k"]. Now print the letter “j” in alert
// using array index

// var alphabet = ["h","i","j","k"];
// alert(alphabet[2]);

// Q4. var alphabet=["h","i","j","k", “l”,”m”, “n”, “o”]. Find the total
// length of array.

// var alphabet = ["h","i","j","k", "l","m", "n", "o"];
// console.log(alphabet.length);

// Q5. Declare an array with one element and Add a second element
// with index in array. Create an alert whose message is the new
// element.

// var cities = ["Karachi"];
// cities[1] = "Lahore";
// alert(cities[1]);

// Chapter 16 (Array II)

// Q1. Code an array with 1 string element.
// Add an additional element to the array using push.
// Create an alert whose message is the last element.

// var country = ["Pakistan"];
// country.push("China");
// alert(country[country.length - 1]);

// Q2. var Alphabet=["h","i","j","k"]
// Remove the last element from the array Alphabet.

// var Alphabet = ["h","i","j","k"];
// Alphabet.pop();

// Q3. var Alphabet=["h","i","j","k"]
// Add a new element, a number, to the end of an array.

// var Alphabet = ["h","i","j","k"];
// Alphabet.push("car","1");

// Chapter 16 (Array III)

// Q1. var sizes = ["S", "M", "XL", "XXL", "XXXL"].
// Remove the first element of an array.

// var sizes = ["S", "M", "XL", "XXL", "XXXL"];
// sizes.shift();

// Q2. var sizes = ["S", "M", "XL", "XXL", "XXXL"].
// Add three number elements to the beginning of an array.

// var sizes = ["S", "M", "XL", "XXL", "XXXL"];
// sizes.unshift(10,11,12);

// Q3. Declare an array with one element.
// Add a second element to the beginning of the array.
// Create an alert whose message is the new first element.

// var colours = ["green"];
// colours.unshift("red");
// alert(colours[0]);

// Q4. var sizes = ["S", "M", "XL", "XXL", "XXXL"].
// Insert "L" into the array between "M" and "XL".

// var sizes = ["S", "M", "XL", "XXL", "XXXL"];
// sizes.splice(2,0,"L");

// Q5. var sizes = ["S", "M", "XL", "XXL", "XXXL"].
// Copy the first 3 sizes of the array and put them into a new array,
// regSizes.

// var sizes = ["S", "M", "XL", "XXL", "XXXL"];
// var regSizes = sizes.slice(0,3);

// Q6. var pets = ["dog", "cat", "ox", "duck", "frog"].
// Add 2 elements after "dog" and remove "cat", "ox", and "duck".

// var pets = ["dog", "cat", "ox", "duck", "frog"];
// pets.splice(1,3,"snake","chicken");

// Q7. var pets = ["dog", "cat", "ox", "duck", "frog"];
// Remove "cat" and "ox".

// var pets = ["dog", "cat", "ox", "duck", "frog"];
// pets.splice(1,2);

// Q8. var pets = ["dog", "cat", "ox", "duck", "frog", "flea"];
// Reduce it to "duck" and "frog" using slice.

// var pets = ["dog", "cat", "ox", "duck", "frog", "flea"];
// var newPet = pets.slice(3,5);

// Chapter 17 - 20 (for Loops)

// Q1. Write a statement in which loop is to run 10 times.

// for(var i = 0; i < 10; i++) {

// }

// Q2. Code the first line of a for loop with the usual counter name, usual
// starting value, and usual increment. Run it 12 times using <= to
// specify how many loops.

// for(var a = 1; a <= 12; a++) {
//     console.log("Hello");
// }

// Q3. What are the 5 characters missing from this code, excluding any
// spaces that are missing? Type them in order, with no spaces or
// commas between them.
// for var i = 0 i <= 4 i++

// for(var i = 0; i <= 4; i++) {

// }

// Q4. Code the first line of a for loop with a counter name that's not i.
// Code the usual starting value and usual increment. Run it 100
// times using < to specify how many loops.

// for (var j = 0; j < 100; j++) {
//     console.log("Hello World");
// }  

// Q5. Code the first line of a for loop with the usual counter and the
// usual starting value. Run it 3 times using > to specify how many
// loops. Decrement it with each iteration.

// for(var i = 3; i > 0; i--) {
//     console.log("loop");
// }

// Q6. The statement assigns the number of elements in the array to the
// variable.

// var alphabet = ["a","b","c","d","e"];
// var numberOfElements = alphabet.length;
// console.log(numberOfElements);

// Q7. Set a variable named “flag” with an initial Boolean value of your
// choice.

// var flag = true;

// Q8. Code the first line of a for loop with the usual counter, the usual
// starting value, and the usual incrementing. Limit the number of
// loops by the number of elements in the array pets.

// var pets = ["dog", "cat", "ox", "duck", "frog", "flea"];
// for(var i = 1; i <= pets.length; i++) {
//     console.log(i);
// }

// Q9. Coding Exercise:
// Set a for loop to run 10 iterations.
// On the second iteration, display the counter in an alert. (It should
// be 1.)
// Break out of the loop.

// for (var i = 0; i < 10; i++) {
//    if (i === 1) {
//     alert(i);
//     break;
//    }
// }  

// Q10. Create an array which contains user names
// Code a prompt with the message "Enter first name". The user's
// response is assigned to firstName.
// Code the first line of a for loop with the usual counter, the usual
// starting value, and the usual incrementing. Limit the number of
// loops by the number of elements in the array user names.
// Code an if statement that tests the presense of (user name) in an
// array.
// If user name match Alert that "Enter". if not then alert ("Please
// write correct user name").

// var firstName = prompt("Enter your name");
// var userNames = ["ali","ahmed","zubair"];
// for(var i = 0; i < userNames.length; i++) {
//     if (firstName === userNames[i]) {
//         alert("Enter");
//         break;
//     } else {
//         alert("Please write correct user name");
//         break;
//     }
// }

// Q11. Complete this code to display an alert if a match isn't found.
// var matchFound = false;
// for (var i = 0; i < list.length; i++) {
//  if (userInput === list[i]) {
//  alert("Match found");
//  matchFound = true;
//  break;
//  }
// };

// var matchFound = false;
// for (var i = 0; i < list.length; i++) {
//  if (userInput === list[i]) {
//  alert("Match found");
//  matchFound = true;
//  break;
//  }
// }
// if (!matchFound) {
//     alert("No match found");
// }

// Q12. var firstArr = [“a”, “b”, “c”, “d”, “e”, “f”];
// var secondArr = [1, 2, 3, 4, 5, 6];
// Code the first line of a for loop with the usual counter, the usual
// starting value, and the usual incrementing. Limit the number of
// loops by the number of elements in the array firstArr.
// In the scope of main loop Code the nested loop. Limit the number
// of nested loops by the number of elements in the array
// secondArr.
// After that concatenate the both loops.
// Expected Output:
// a1
// a2
// a3
// a4
// …
// f6

// var firstArr = ["a","b","c","d","e","f"];
// var secondArr = [1, 2, 3, 4, 5, 6];
// for(var j = 0; j < firstArr.length; j++) {
//     for(var e = 0; e < secondArr.length; e++) {
//  console.log(firstArr[j] + secondArr[e]);       
//     }
// }

// **********************************Chapter 21 (Changing Case)********************************** 

// Q1. Type the characters that are missing from this code.
// var allLower = userInput.toLowerCase;
// Note: Correct this statement by yourself.

// var allLower = userInput.toLowerCase();\

// Q2. Convert the string represented by x to lower-case and assign the
// result to the same variable.

// var x = "HELLO WORLD";
// x = x.toLowerCase();
// console.log(x);

// Q3. Convert the string represented by y to upper-case and assign the
// result to the same variable.

// var y = "country";
// y = y.toUpperCase();
// console.log(y);

// Q4. Convert the string represented by a variable to lower-case and
// assign the result to a second variable that hasn't been declared
// beforehand.

// var a = "HELLO WORLD";
// var b = a.toLowerCase();

// Q5. Convert the string represented by an array element to lower-case
// and assign it to a variable that hasn't been declared beforehand.

// var a = ["APPLE","ORANGE","MANGO"];
// var b = a[0].toLowerCase();
// console.log(b);

// Q6. Display in an alert the upper-case version of the string
// represented by a variable.

// var lowerCaseString = "hello world!";
// var upperCaseString = lowerCaseString.toUpperCase();
// alert(upperCaseString);

// Q7. var cityName = “kaRacHi”;
// Convert the string represented by a cityName in Capitalisation is
// the writing of a word with its first letter in uppercase and the
// remaining letters in lowercase.

// var cityName = "kaRacHi";
// var capitalizedCityName = cityName.charAt(0).toUpperCase() + cityName.slice(1,7).toLocaleLowerCase();
// console.log(capitalizedCityName);

// **********************************Chapter 22 - 25 (Strings)**********************************

// Q1. "captain" has been assigned to variable “sameWords”. You want
// to slice "ap" 

// var sameWords = "captain";
// var slicedWord = sameWords.slice(0,3);
// console.log(slicedWord);

// Q2. The number of characters in the string will be assigned to the
// variable.

// var country = "Pakistan";
// var numOFChar = country.length;
// console.log(numOFChar);

// Q3. The string "elephant" has been assigned to the variable animal.
// Slice the four middle characters out of the string and assign it to
// the variable seg, which hasn't been declared beforehand.

// var animal = "elephant"
// var seg = animal.slice(2,6);
// console.log(seg);

// Q4. Find the number of characters in the string represented by a
// variable and assign the number to a second variable.

// var fruit = "Mango";
// var numOfChar = fruit.length;
// console.log(numOfChar);

// Q5. In a first statement measure how many characters there are in a
// string represented by a variable. In a second statement slice all
// but the first character and last 3 characters of the string and
// assign it to a second variable that hasn't been declared
// beforehand.

// var city = "Islamabad";
// var numOfChar = city.length;
// console.log(numOfChar);
// var slicedWord = city.slice(1,-3);
// console.log(slicedWord);

// Q6. var text = "To be or not to be.";
// var indx = text.indexOf("be");
// What is the value of indx?

// The value of indx is "3";

// Q7. var text = "To be or not to be.";
// var indx = text.lastIndexOf("be");
// What is the value of indx?
// Note: Try the above both examples by yourself.

// The value of indx is "16";

// Q8. Find the index of the first character of the last instance of "go" in
// the string represented by the variable text and assign the number
// to the variable indx, which hasn't been declared beforehand.

// var text = "To go or not to go";
// var indx = text.lastIndexOf("go");
// console.log(indx);

// Q9. Code the first line of an if statement that tests whether a segment
// with an index represented by indexNum exists in a string.



// Q10. In this string "abcde", what character is at index 2? (Use
// charAt)

// var string = "abcde";
// var charAtIndex2 = string.charAt(2);
// console.log(charAtIndex2);
// Answer is "c"

// Q11. Find the 10th character in the string represented by text and
// assign it to the variable cha, which hasn't been declared
// beforehand.

// var text = "Lorem ipsum dolor sit amet."
// var cha = text.charAt(9);
// console.log(cha);

// Q12. Find the last character in the string represented by str and
// assign it to x, which hasn't been declared beforehand.

// var str = "Javascript";
// var x = str.charAt(str.length -1);
// console.log(x);

// Q13. Find the the 5th character in a string represented by input
// and assign it to cha, which hasn't been declared beforehand.

// var input = "Karachi"
// var cha = input.charAt(4);
// console.log(cha);

// Q14. Code the first line of an if statement that tests whether the
// 3rd character of a string represented by a variable is a particular
// character.

// if (pet.charAt(2) === "t") {

// }

// Q15. Code a for loop that cycles through all the characters of a
// string represented by a variable and assigns each character to an
// element of an array that has been declared beforehand.
// In the string represented by reply replace the first instance of "no"
// with "yes" and assign the revised string to revisedReply, which hasn't
// been declared beforehand.

// var originalString = "YourOriginalString";
// var characterArray = [];

// for (var i = 0; i < originalString.length; i++) {
//   characterArray.push(originalString.charAt(i));
// }

// Now characterArray contains each character of the original string

// To replace the first instance of "no" with "yes" and assign it to revisedReply
// var reply = "Replace the first no, not the second no.";
// var revisedReply = reply.replace("no", "yes");

// console.log(revisedReply);


// Q16. In a string represented by str replace the first instance of "1"
// with "one" and assign the revised string to newStr, which hasn't
// been declared beforehand.

// var str = "1";
// var newStr = str.replace("1","one");
// console.log(newStr);


// Q17. If you want all instances replaced, enter 3 characters that
// need to appear in this statement.
// var y = x.replace("a", "z");

// var y = x.replace(/a/g, "z");

// **********************************Chapter 26 (Rounding Numbers)**********************************

// Q1. Form a statement that rounds a number to the nearest integer.

// var num = 1.5;
// var roundNum = Math.round(num);
// console.log(roundNum);

// Q2. Round up a number represented by origNum and assign it to
// roundNum, which hasn't been declared beforehand.

// var origNum = 3.1;
// var roundNum = Math.ceil(origNum);
// console.log(roundNum);

// Q3. Round down a number represented by origNum and assign it to
// roundNum, which hasn't been declared beforehand.

// var origNum = 4.9;
// var roundNum = Math.floor(origNum);
// console.log(roundNum);

// Q4. Round a number represented by a variable and assign the result
// to a second variable that hasn't been declared beforehand.

// var origNum = 4.5;
// var roundNum = Math.round(origNum);
// console.log(roundNum);

// Q5. Round .5 to 0 and assign it to a variable that hasn't been declared
// beforehand.

// var origNum = .5;
// var roundNum = Math.floor(origNum);
// console.log(roundNum);

// **************************************Chapter 27 (Random Numbers)**************************************

// Q1. Convert a random number generated by JavaScript to a number in
// the range 1 to 50

// var randomNum = Math.floor(Math.random() * 50 ) + 1;
// console.log(randomNum); 

// Q2. Generate a random number and assign it to a variable that hasn't
// been declared beforehand.

// var num = Math.random();

// Q3. You have to create a dice in JavaScript with the use of pseudorandom number.

// var dice = Math.floor(Math.random() * 6) + 1;
// console.log("You rolled a " + dice);


// Q4. You have to create a toss (head/tail) in JavaScript with the use of
// pseudo-random number.

// var randomNum = Math.round(Math.random());
// if (randomNum === 0) {
//     alert("head");
// } else {
//     alert("tail");
// }
// console.log(randomNum);

// *********************************Chapter 28, 29 (Converting Strings)*********************************

// Q1. How do you convert a string to an integer in JavaScript?

// var str = "42";
// var num = parseInt(str);
// console.log(num);

// Q2. Write a JavaScript function to convert the string "123" to an
// integer.

// var str = "123";
// function stringToInt(str) {
//     var int = parseInt(str);
//     return int;
// }
// var result = stringToInt(str);
// console.log(result);

// Q3. How can you convert a string containing a decimal number to a
// floating-point number in JavaScript?

// var str = "2.54";
// var floatNumber = parseFloat(str);
// console.log(floatNumber);

// Q4. How can you check if a string can be successfully converted to an
// integer or decimal in JavaScript before performing the
// conversion?



// Q5. How can you convert a number to a string in JavaScript?

// var num = 5;
// var str = num.toString();
// console.log(typeof str);

// Q6. Write a JavaScript function to convert the number 42 to a string.

// var num = 42;
// function numToStr(num) {
//     var str = num.toString();
//     return str;
// }
// var result = numToStr(num);
// console.log(typeof result);

// Q7. Can you convert a string representing a decimal number (e.g.,
// "3.14") to an integer in JavaScript? If so, how?

// var decimalString = "3.14";
// var integerNumber = parseInt(decimalString);
// console.log(integerNumber);

// ****************************Chapter 30 (Controlling the length of decimals)****************************

// Q1. Code a statement that rounds a number represented by num to 4
// places, converts it to a string, and assigns it to newNum, which
// hasn't been declared beforehand.

// var num = 2.12354798748937;
// var newNum = num.toFixed(4);
// console.log(newNum);

// Q2. In a single statement round a number represented by a variable to
// 2 places, convert it to a string, convert it back to a number, and
// assign it to the same variable.

// var num = 3.3242343242;
// var newNum = parseFloat(num.toFixed(2));
// console.log(newNum);
// console.log(typeof newNum);

// Q3. Code the first line of an if statement that tests whether the
// number represented by num, rounded to 2 digits and converted
// to a string, has more than 4 characters in it.

// var num = 1.1877777777777777777777777777777777777777777777;
// if (num.toFixed(2).toString().length > 4) {
//     alert(num)
// }  

// Q4. Assign a number with many decimal places to a variable.
// Code an alert that displays the number rounded to 2 decimal
// places and converted to a string.

// var num = 20.456789;
// var roundedNum = num.toFixed(2);
// alert(roundedNum);

// ***********************************Chapter 31 - 34 (Date & Time)***********************************

// Q1. Code a statement that creates a new Date object and assigns it to
// dObj, which hasn't been declared beforehand.

// var dObj = new Date();

// Q2. Code a statement that creates a new Date object, converts it to a
// string, and assigns the string to dStr, which hasn't been declared
// beforehand.

// var dStr = new Date().toString();

// Q3. Code a statement that extracts the day of the week from a Date
// object represented by d and assigns it to day, which hasn't been
// declared beforehand.

// var d = new Date();
// var day = d.getDay();
// console.log(day);

// Q4. The day has been extracted from the Date object and assigned to
// d. The names of the days of the week have been assigned to the
// array dayNames. Alert the current day with array index.

// var day = "Sunday"
// var dayNames = ["Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"];
// var index = dayNames.indexOf(day);
// if (index !== -1) {
//   alert("The current day is: " + dayNames[index]);
// } else {
//   alert("Invalid day");
// }


// Q5. Extract all parts of the Date and Time and assign it to the variable
// which has not been declared beforehand.

// var currentDate = new Date();
// var year = currentDate.getFullYear();
// var month = currentDate.getMonth() + 1;
// var day = currentDate.getDate();
// var hours = currentDate.getHours();
// var minutes = currentDate.getMinutes();
// var seconds = currentDate.getSeconds();
// console.log("Year:", year);
// console.log("Month:", month);
// console.log("Day:", day);
// console.log("Hours:", hours);
// console.log("Minutes:", minutes);
// console.log("Seconds:", seconds);

// Q6. Code a statement that creates a Date object for the last day of the
// last month of 2020 and assigns it to later, which hasn't been
// declared beforehand.

// var date = new Date(2020,11,31);
// console.log(date);

// Q7. Create a Date object for the second day of the second month of
// 1992 and assign it to a variable that hasn't been declared
// beforehand.

// var date = new Date(1992,1,2);
// console.log(date);

// Q8. Code a single statement that displays in an alert the milliseconds
// that elapsed between the reference date and the beginning of
// 1980.

// alert(new Date(1980, 0, 1).getTime());

// Q9. How do you change the year of a date in JavaScript?

// var date = new Date();
// date.setFullYear(2000);
// console.log(date);

// Q10. Write a JavaScript function to change the month of a given
// date to January.

// var date = new Date();
// var newDate = changeMonth(date);
// function changeMonth(date) {
//   date.setMonth(0);
//    return date;
// }
// console.log(newDate);

// Q11. What is the method to change the day of the week for a
// specific date in JavaScript?

// var date = new Date();
// var day = date.getDay() - 2;
// console.log(day);

// Q12. Write a JavaScript function to change the minutes of a given
// time to a specific value. (Value by prompt)

// var date = new Date();
// var changeValue = +prompt("Enter value to change the Min");
// function changeMin(date) {
//   date.setMinutes(changeValue);
//   var newDate = date;
//   console.log(newDate);
// }


// Q13. Write a JavaScript function to add a specific number of
// hours to a given time.



// Q14. You have to create a age calculator in JavaScript.

// function calculateAge(birthdate) {
//   var birthDate = new Date(birthdate);

//   var currentDate = new Date();

//   var ageInMilliseconds = currentDate - birthDate;
//   var ageInYears = Math.floor(ageInMilliseconds / (365 * 24 * 60 * 60 * 1000));

//   return ageInYears;
// }

// var birthdate = "1990-01-15"; 
// var age = calculateAge(birthdate);
// console.log("Age:", age); 

// **********************************Chapter 35 - 37 (Functions)**********************************

// 1. Code the first line of a function displayAlert.

// function greet() {
//   alert("Hello");
// }
// greet();

// 2. Code a function named askName that prompts the user to "Enter
// name" and assigns the answer to userName, which hasn't been
// declared beforehand.

// function askName() {
//   var userName = prompt("Enter your name");
// }

// 3. Code a function that calls 2 other functions.

function mainFunction() {
  var result1 = firstFunction();
  var result2 = secondFunction();
  // var combinedResult = result1 + result2;

  // Return the final result
  // return combinedResult;
}
function firstFunction() {
  // This is the first function
  var value1 = 5;
  var value2 = 10;
  return value1 + value2;
}
function secondFunction() {
  // This is the second function
  var value1 = 15;
  var value2 = 20;
  return value1 * value2;
}
// Call the main function to execute both firstFunction and secondFunction
var finalResult = mainFunction();
console.log("Final Result:", finalResult); // Display the final result


// 4. Code a function that displays a prompt, "Enter name" and then
// displays the name in an alert. Call the function.
// 5. Code the first line of a function named concat that has 3
// parameters, the first three letters of the alphabet. Call that takes
// a variable, a string, and a number as arguments.
// 6. Code a function that has 2 parameters. Concatenate them and
// assign the result to a variable that hasn't been declared
// beforehand.
// 7. Code a function that has three parameters. Multiply them and
// assign them to a variable that hasn't been declared yet.
// 8. Write a JavaScript function that takes an array of numbers as
// input and returns the average of those numbers.
// 9. Write a JavaScript function that takes two parameters and returns
// their sum.
// 10. Write a JavaScript function that takes an array of numbers as
// input and returns the average of those numbers.
// 11. You have to capture the returned value from a function and
// store it in a variable?
// 12. Write a function which tells letter counts of (word).
// 13. Write a function to set (year) in date object.
// 14. Write a function which tells the age of a person who Born on
// (dateOfBirth)
// 15. Write a function which tells the presense of (word) in an
// array = ['zaid','haris','raza','abubakar','hassan','hussain','fatima']
// result should be in true or false
// 16. Write a function which repeat (letter) 10 times.
// Hint: "abcde" str.repeat(10)
// 17. write a function which reverse array = ['a','b','c','d','e']
// Hint: arr.reverse()
